#include <stdio.h>

/**
 * main - this is the main function
 *
 * Return: if successful, should be zero
 */

int main(void)

{
	int s;

	for (s = 0 ; s < 10 ; s++)
		printf("%d", s);
	printf("\n");
	return (0);
}
